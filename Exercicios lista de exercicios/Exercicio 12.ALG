algoritmo "Exercicio 12"
var
   produto, calculo, calculo2 : real
inicio
         escreval("Insira o valor do produto : ")
         leia(produto)
         calculo <- produto*5/100
          calculo2 <- produto-calculo
          escreval("O valor com desconto sera de : ",calculo2)

fimalgoritmo